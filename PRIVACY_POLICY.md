# 하루일과 개인정보 처리방침 (초안)

**시행일 / 최종 개정일**: [YYYY-MM-DD — 배포 전 확정 필요]

이 문서는 초안입니다. Google Play 등록 전 다음을 반드시 확인하세요.

- [ ] 아래 `[대괄호]` 표시된 자리를 실제 값으로 채운다(연락처, 개발자/회사명, 시행일).
- [ ] 이 문서를 사용자가 접근 가능한 URL로 게시한다(GitHub Pages, 개인 웹사이트 등) — Play Console은 정책 문서를 앱 내 텍스트가 아니라 **URL**로 요구한다.
- [ ] 그 URL을 Play Console의 "개인정보처리방침" 항목과 앱 내 설정 화면(현재 `settings_privacy_notice_message`)에 반영한다.
- [ ] 이후 앱에 서버 연동·로그인·분석 SDK 등이 추가되면 이 문서도 함께 갱신한다(현재는 2026-08-20 기준 로컬 전용 버전만을 설명한다).

---

## 1. 개요

**하루일과**(패키지명 `com.haruilgwa.app`)는 사용자가 스스로 세운 하루 일정과 활동을 기록하고, 계획한 시간을 실제로 지켰는지 판정해 보여주는 개인용 일정·시간 관리 앱입니다.

**이 앱은 현재 전적으로 기기 내부에서만 동작합니다.** 서버 계정, 로그인, 클라우드 동기화, 분석(애널리틱스) SDK, 광고 SDK를 포함하지 않으며, 앱에는 인터넷 접근 권한(`INTERNET`) 자체가 선언되어 있지 않아 네트워크로 어떤 데이터도 보낼 수 없습니다.

## 2. 수집·저장하는 정보

아래 정보는 모두 **사용자의 기기 안에 있는 로컬 데이터베이스에만** 저장되며, 개발자를 포함한 어떤 외부 서버로도 전송되지 않습니다.

| 구분 | 내용 |
| --- | --- |
| 프로필 | 사용자가 직접 입력한 닉네임, 직업(선택 입력) |
| 활동·일정 | 활동/일정 이름, 카테고리, 메모, 예정 시각, 반복 규칙, 알림 설정, 중요 표시 여부 |
| 실행·판정 기록 | 실제 시작·종료 시각, 진행 시간(타이머 기준), 자가 평가한 집중도, 성공/부분성공/실패 판정 결과와 그 근거 |
| 집중 사이클 기록 | 공부·휴식 단계별 진행 상태와 시각 |
| 설치 식별자 | 앱을 처음 실행할 때 기기에서 무작위로 생성하는 UUID 1개. 전화번호·이메일·광고 식별자·기기 하드웨어 ID가 아니며, 여러 항목을 같은 기기에서 만들었다는 것만 구분하는 용도로 내부적으로만 쓰인다 |
| 알림 예약을 위한 내부 값 | 알림·알람을 안정적으로 예약하기 위한 내부 정수 값(개인을 식별하지 않는다) |

## 3. 수집하지 않는 정보

- 위치(GPS) 정보 — 위치 권한을 요청하지 않으며 관련 기능이 꺼져 있습니다.
- 연락처, 사진, 마이크, 카메라
- 광고 식별자(Advertising ID)
- 이용 통계·분석(애널리틱스) 데이터, 크래시 리포트
- 결제 정보

## 4. 알림

일정 알림은 전부 **기기 내부에서 예약되는 로컬 알림**이며, 푸시 알림 서비스(FCM 등)를 사용하지 않습니다. 설정에서 "알림에 일정명 표시"를 꺼두면(기본값) 잠금 화면·알림에는 실제 일정명 대신 일반 문구만 표시됩니다.

## 5. 데이터를 다른 곳으로 보내지 않습니다

이 앱은 `INTERNET` 권한을 선언하지 않았으므로, 어떤 코드 경로로도 사용자의 데이터를 외부 서버나 제3자에게 전송할 수 없습니다. 따라서:

- 서버로 전송되는 데이터가 없습니다.
- 제3자와 공유되는 데이터가 없습니다.
- 광고 목적으로 사용되는 데이터가 없습니다.

## 6. 데이터 내보내기

설정 화면의 "일정·활동·결과 내보내기(JSON)"를 사용하면 사용자가 직접 선택한 위치(기기 저장소, 사용자가 고른 클라우드 폴더 등)에 JSON 파일을 저장할 수 있습니다. 이 파일에는 닉네임, 메모, 일정·활동 이름, 실행 결과가 그대로 포함되므로, 앱은 저장 전에 포함 데이터를 안내하는 확인창을 보여줍니다. 이 내보내기는 사용자가 직접 실행해야만 일어나며, 앱이 자동으로 파일을 만들거나 전송하지 않습니다.

## 7. 데이터 보관 및 삭제

- 데이터는 사용자가 직접 삭제하거나 앱을 기기에서 삭제할 때까지 기기에 남아 있습니다.
- 설정 화면의 "모든 로컬 데이터 삭제"를 사용하면 저장된 모든 일정·활동·기록과 진행 중인 세션이 즉시 영구 삭제됩니다(되돌릴 수 없음).
- 앱은 Android 자동 백업(`allowBackup`)에서 제외되어 있어, 기기 백업·복원 과정에서도 이 데이터가 클라우드로 전송되지 않습니다.

## 8. 아동의 개인정보

이 앱은 특정 연령층(아동)을 대상으로 만들어지지 않았으며, 연령 확인 절차나 아동 대상 맞춤 기능을 포함하지 않습니다.

## 9. 향후 변경 예정 사항

로드맵상 서버 계정 로그인·동기화 기능이 검토되고 있습니다(현재 미착수). 그런 기능이 실제로 추가되면 이 문서를 먼저 갱신하고, 어떤 데이터가 어떤 목적으로 서버에 전송·저장되는지, 사용자가 어떻게 동의를 철회하고 삭제를 요청할 수 있는지를 명시한 뒤 배포합니다.

## 10. 이 방침의 변경

이 방침이 바뀌면 상단의 "최종 개정일"을 갱신합니다. 저장소(`SECURITY_AND_ROADMAP.md`)에서 변경 이력을 함께 추적합니다.

## 11. 문의처

- 개발자/서비스명: 노아 (하루일과)
- 연락처: [이메일 주소 — 출시 전 확정 필요]
